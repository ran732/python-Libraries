# -*- coding: utf-8 -*-
"""Numpy_Fundamentals.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1A-tLtLz0QEgF9FOLZ1d7-DXKWJ48KpYM

# What is numpy?

```
# This is formatted as code
```


**NumPy** is the fundamental package for scientific computing in Python. It is a Python library that provides a multidimensional array object, various derived objects (such as masked arrays and matrices), and an assortment of routines for fast operations on arrays, including mathematical, logical, shape manipulation, sorting, selecting, I/O, discrete Fourier transforms, basic linear algebra, basic statistical operations, random simulation and much more.

At the core of the NumPy package, is the ndarray object. This encapsulates n-dimensional arrays of homogeneous data types

## **Numpy Arrays Vs Python Sequences**

- NumPy arrays have a fixed size at creation, unlike Python lists (which can grow dynamically). Changing the size of an ndarray will create a new array and delete the original.

- The elements in a NumPy array are all required to be of the same data type, and thus will be the same size in memory.

- NumPy arrays facilitate advanced mathematical and other types of operations on large numbers of data. Typically, such operations are executed more efficiently and with less code than is possible using Python’s built-in sequences.

- A growing plethora of scientific and mathematical Python-based packages are using NumPy arrays; though these typically support Python-sequence input, they convert such input to NumPy arrays prior to processing, and they often output NumPy arrays.
"""

#np.array
import numpy as np

a = np.array([1,2,3,4,5])
print(a)

#2D and 3D

b = np.array([[1,2,3],[4,5,7]])
print(b)

c = np.array([[[1,2],[3,4]],[[4,5],[7,8]]])
print(c)

# d type
np.array([1,2,3,4],dtype=float)

np.array([1,2,3,4],dtype=bool)

np.array([1,2,3,4],dtype=complex)

#np.arange
np.arange(1,11)

# np.reshape
 np.arange(1,9).reshape(2,2,2)

#np.ones and np.zeros
np.zeros((3,4))

#np.random
np.random.random((3,4))

#np.linspace
np.linspace(-10,10,10)  #equidistance point generator in given range

#np.identity
np.identity(5)

"""# **Numpy Attributes**"""

a1=np.arange(10)
a2=np.arange(12,dtype=float).reshape(3,4)
a3=np.arange(8).reshape(2,2,2)

a2

#ndim
a3.ndim

#shape
a3.shape

#size
a3.size

#itemsize
a3.itemsize

#dtype
a2.dtype

"""# **Changing Datatypes**"""

#astype
#reduce size
a2.astype(np.int32)

a2.itemsize

"""# **Array Operations**"""

a1 = np.arange(12).reshape(3,4)
a2 = np.arange(12,24).reshape(3,4)

a2

#scalar operations

#arithmatic ( +,-,*,/,%)
a1 % 2

#relational (>,<,>=,<=,!=)
a1>5

#vector operations

# arithmatic
a2 + a1  #squre matrix must be

"""# **Array Functions**"""

a1= np.random.random((3,3))
a1 = np.round(a1*100)
a1

#max/min/sum/prod/
# 0--> col and 1--> row
np.max(a1)
np.max(a1,axis=0)

np.min(a1)
np.min(a1,axis=0)

np.sum(a1)

np.prod(a1)

# mean/median/std/var
np.mean(a1)
np.var(a1,axis=1)

#trigonomatry functions
np.sin(a1)

# dot product (3,4 . 4,2 = 3,2)

 a2=np.arange(12).reshape(3,4)
 a3=np.arange(12,24).reshape(4,3)
 np.dot(a2,a3)

#log and exponents
np.log (a1)

np.exp(a1)

#round/floor/cell
# round -- > convert into nearest integer
#floor -- > back integer
#ceil -- > front integer

np.ceil(np.random.random((2,3))*100)

"""# **Indexing and Slicing**"""

a1=np.arange(10)
a2=np.arange(12).reshape(3,4)
a3=np.arange(27).reshape(3,3,3)

a3

a2[0]  # row
a2[0][2] # row , col
a2[0,2]  # row , col

a3[1,0,1]   #5
a3[0,1,0]   #2
a3[1,1,1]   #7

a2

a1[2:5]  #2,3,4
a1[2:5:2]

a2[0,:]
a2[:,2]
a2[1,1:] #5,6,7
a2[2,1:3] #9,10
a2[::2,::3] #0,3,8,11
a2[::2,1::2] #1,3,9,11
a2[1,::3]#4,7
a2[:2,1:] #1,2,3,5,6,7

a3

a3[1,] #
a3[0::2]
a3[0,1,:] #3,4,5
a3[1,:,1] #10,13,16
a3[2,1:,1:] #22,23,25,26
a3[::2, 0,::2] #0,2,18,22

"""# **Iterating**"""

a1

for i in a1:
  print(i)

for i in a2:
  print(i)

for i in a3:
  print(i)

for i in np.nditer(a3):
  print(i, end =' ')

"""# **Reshaping**"""

#reshape

"""Transpose concert row into col and vice-versa

"""

# tranpose
np.transpose(a2)
a2.T # sort m hhaa🙃

"""ravel convert higher dimention to 1D array"""

#ravel
a2.ravel()

"""# **Stacking**  : add krna

"""

a4=np.arange(12).reshape(3,4)
a5=np.arange(12,24).reshape(3,4)
a5

# horizontal stacking
np.hstack((a4,a5,a4))

# vertical stacking
np.vstack((a4,a5,a4))

"""# **Splitting**  : opposite of stacking"""

a4

# horizontal splitting

np.hsplit(a4,4)

# vertical splitting

np.vsplit(a4,3)

